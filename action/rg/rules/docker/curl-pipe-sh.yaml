rules:
  - id: rg.docker.curl-pipe-sh
    languages: [dockerfile]
    message: "Avoid piping curl to shell (curl | sh). Pin checksums and verify signatures."
    severity: ERROR
    pattern: "curl ... | sh"
